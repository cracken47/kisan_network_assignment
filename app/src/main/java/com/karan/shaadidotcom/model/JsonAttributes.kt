package com.karan.shaadidotcom.model

/**
 * Used as keys to read values from contact json file
 */
interface JsonAttributes {
    companion object {
        const val FULL_NAME = "full_name"
        const val COUNTRY_CODE = "country_code"
        const val MOBILE_NUMBER = "number"
    }
}